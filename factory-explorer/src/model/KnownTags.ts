enum KnownTags {
    /** The resources required for this recipe cannot be created automatically. They must be collected by the player via cutting down trees  or killing creatures etc. */
    Finite = 'finite',
    Manual = 'manual',
    Pure = 'pure',
    Normal = 'normal',
    Impure = 'impure',
    Generator = 'generator',
    Extractor = 'extractor',
    Ficsmas = 'ficsmas',
    Preferred = 'preferred',
};

export default KnownTags;
